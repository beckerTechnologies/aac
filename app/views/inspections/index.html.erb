<div class="sign_in">
<h2>All Inspections</h2>

<%= form_tag("/inspections", :method => "get") do %>
  <p class="search">
    <%= text_field_tag :search, params[:search] %>
    <%= submit_tag "Search VIN", :name => nil %>
  </p>
<% end %>

<div class="table_css">
<table>
  <thead>
    <tr>
      <th>VIN Number</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @inspections.each do |inspection| %>
      <tr>
        <td><span class="smaller_text"><%= inspection.vehicle.vin %></span></td>
        <td><%= link_to 'Show Report', {:controller => "reports", :action => "pdf", :id => inspection.id#, :format => :pdf
            } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>

<br>

<%= button_to 'New Inspection', new_inspection_path, :method => :get %>
</div>
